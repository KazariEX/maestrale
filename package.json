{
  "type": "module",
  "private": true,
  "packageManager": "pnpm@10.15.0",
  "scripts": {
    "build": "pnpm --filter maestrale... build",
    "dev": "pnpm --filter maestrale... --parallel dev",
    "prepare": "zin link core && pnpm run build",
    "release": "bumpp -r --no-push -c \"release: v%s\"",
    "test": "pnpm -r test",
    "assets:check": "tsx scripts/assets.ts"
  },
  "devDependencies": {
    "@antfu/eslint-config": "catalog:",
    "@types/node": "catalog:",
    "@zinkawaii/eslint-config": "catalog:",
    "@zinkawaii/scripts": "catalog:",
    "@zinkawaii/stylelint-config": "catalog:",
    "@zinkawaii/tsconfig": "catalog:",
    "bumpp": "catalog:",
    "consola": "catalog:",
    "eslint": "catalog:",
    "stylelint": "catalog:",
    "tsdown": "catalog:",
    "tsx": "catalog:",
    "vitest": "catalog:"
  }
}
